{
  "subject": "cdk_synth",
  "template_glob": "cdk.out/*.template.json",
  "checks": [
    "INFRA-005.01-waf-rate-rule-limit-positive",
    "INFRA-005.02-trusted-viewer-ip-custom-key"
  ],
  "correlation_obligations": [
    "The regional WAF web ACL is associated with the internal ALB and its positive-limit CUSTOM_KEYS rate rule aggregates the x-incident-viewer-ip header, which an associated CloudFront viewer-request function on every behavior that forwards to the ALB overwrites from event.viewer.ip, replacing any client-supplied value."
  ]
}
