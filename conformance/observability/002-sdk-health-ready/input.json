{
  "scenario": "sdk-health-ready",
  "contract": "GET /sdk/healthz returns exact 200 JSON ok only after SDK server and DynamoDB adapter are ready",
  "protocol_version": "2026-07-28",
  "providers": [
    "raw",
    "sdk"
  ]
}
