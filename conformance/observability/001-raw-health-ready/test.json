{
  "spec_id": "OBS-001",
  "description": "GET /raw/healthz returns exact 200 JSON ok only after raw server and DynamoDB adapter are ready",
  "description_bdd": {
    "given": "the raw health ready scenario",
    "when": "the observability contract is exercised",
    "then": "gET /raw/healthz returns exact 200 JSON ok only after raw server and DynamoDB adapter are ready"
  },
  "boundary": "trace-span",
  "normalisation": [
    "timestamp",
    "generated_id",
    "file_path",
    "unordered_collection"
  ],
  "source": "PRD.md § Non-functional requirements / Observability and Deployment / Local",
  "consumers": [
    "typescript-raw"
  ],
  "providers": [
    "raw"
  ],
  "context": "assurance",
  "approved_by": null,
  "approved_at": null,
  "source_deps": [
    "src/adapters/inbound/health.ts"
  ]
}
