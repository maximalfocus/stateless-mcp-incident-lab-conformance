# observability

Declarative, language-neutral trace-span contracts. `expected.json` is authoritative and must never be auto-updated. The implementation runner evaluates each typed assertion against the selected provider.
