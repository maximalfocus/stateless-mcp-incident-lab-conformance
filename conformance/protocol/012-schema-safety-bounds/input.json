{
  "scenario": "schema-safety-bounds",
  "contract": "JSON Schema 2020-12 validation refuses network refs and rejects schemas beyond configured depth or subschema bounds",
  "protocol_version": "2026-07-28",
  "providers": [
    "raw",
    "sdk"
  ]
}
