# performance

Declarative, language-neutral metric-assertion contracts. `expected.json` is authoritative and must never be auto-updated. The implementation runner evaluates each typed assertion against the selected provider.
