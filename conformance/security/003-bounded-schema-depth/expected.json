{
  "assertions": [
    {
      "type": "contract",
      "subject": "bounded-schema-depth",
      "must": "Schema validation rejects depth beyond the configured bound before evaluation"
    },
    {
      "type": "strict_http_shape",
      "undeclared_body_fields": "fail",
      "undeclared_headers": "allowed_only_by_marker"
    }
  ]
}
