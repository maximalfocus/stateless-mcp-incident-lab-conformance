{
  "spec_id": "SEC-003",
  "description": "Schema validation rejects depth beyond the configured bound before evaluation",
  "description_bdd": {
    "given": "the bounded schema depth scenario",
    "when": "the security contract is exercised",
    "then": "schema validation rejects depth beyond the configured bound before evaluation"
  },
  "boundary": "function",
  "normalisation": [
    "timestamp",
    "generated_id",
    "file_path",
    "unordered_collection"
  ],
  "source": "PRD.md § Security posture and Business rules",
  "consumers": [
    "typescript-raw",
    "typescript-sdk"
  ],
  "providers": [
    "raw",
    "sdk"
  ],
  "context": "assurance",
  "approved_by": null,
  "approved_at": null,
  "source_deps": [
    "src/adapters/inbound/http.ts",
    "src/protocol/schema.ts",
    "src/observability/redaction.ts"
  ],
  "type_contract": "validate_json_schema"
}
